<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>mifss-mainline</artifactId>
        <groupId>com.aeye.mifss</groupId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>mifss-intf</artifactId>
    <packaging>pom</packaging>
    <description>MIFSS Interface Layer (接口定义层父工程)</description>

    <!--
        ================================================================================
        接口层规则 (INTERFACE LAYER RULES)
        ================================================================================
        1. 纯净原则 (Purity):
           本层仅包含 POJO (DTO/VO) 和 Dubbo Service 接口定义。
           严禁包含任何业务逻辑实现。
           
        2. 依赖限制 (Dependency Restrictions):
           只能依赖 POJO 相关的基础库 (mifss-commons-core, lombok)。
           严禁引入 Spring Boot Starter, MyBatis, Redis 等重量级依赖。
        ================================================================================
    -->

    <modules>
        <module>mifss-intf-bio</module>
        <module>mifss-intf-ipt</module>
        <module>mifss-intf-opt</module>
        <module>mifss-intf-hd</module>
        <module>mifss-intf-bas</module>
        <module>mifss-intf-pha</module>
        <module>mifss-intf-trt</module>
        <module>mifss-intf-video</module>
        <module>mifss-intf-job</module>
        <module>mifss-intf-api</module>
    </modules>

</project>
